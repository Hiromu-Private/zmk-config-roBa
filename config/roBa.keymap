#include <behaviors.dtsi>
#include <dt-bindings/zmk/bt.h>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/pointing.h>

// OS設定を日本語キーボードのまま使用するための変換定義

#define JP_DQUOTE       AT                // "
#define JP_AMPERSAND    CARET             // &
#define JP_QUOTE        AMPERSAND         // '
#define JP_EQUAL        UNDER             // =
#define JP_CARET        EQUAL             // ^
#define JP_YEN          0x89              // ¥
#define JP_PLUS         COLON             // +
#define JP_TILDE        PLUS              // ~
#define JP_PIPE         LS(0x89)          // |
#define JP_AT           LEFT_BRACKET      // @
#define JP_COLON        SINGLE_QUOTE      // :
#define JP_ASTERISK     DOUBLE_QUOTES     // *
#define JP_BACKQUOTE    LEFT_BRACE        // `
#define JP_UNDERSCORE   LS(0x87)          // _
#define JP_LBRACKET     RIGHT_BRACKET     // [
#define JP_RBRACKET     BACKSLASH         // ]
#define JP_LPAREN       ASTERISK          // (
#define JP_RPAREN       LEFT_PARENTHESIS  // )
#define JP_LBRACE       RIGHT_BRACE       // {
#define JP_RBRACE       PIPE              // }
#define JP_KANA         LANGUAGE_1        // かな
#define JP_EISU         LANGUAGE_2        // 英数
#define JP_HANZEN       GRAVE             // 半角/全角

#define ZMK_POINTING_DEFAULT_SCRL_VAL 100  // #includeのあとのあたりにスクロール量を変更

// ── v2 レイヤー構成（2026-08-14 再設計）─────────────────────
// 5日分のキーログ実測（44,345打鍵）に基づく。設計の記録は
// My_Ops_Hub memory/reference_roba_keymap.md を参照。
//
//   0 default  — ベース（変更なし。, . - ! の筋肉記憶は温存）
//   1 SYM_NUM  — 左親指 SPACE 長押し → 右手に頻出記号
//                （/ @ ; : をホーム段へ。旧 NUM と旧 Sym_Num を統合）
//   2 NUMPAD   — 右親指 SPACE 長押し → 左手テンキー
//                （. , を KP_DOT / KP_COMMA 化＝かなモードでも半角）
//   3 SCROLL   — ENTER 長押し＋トラックボール scroll-layers
//   4 SYS      — Q 長押し → BT・bootloader・Fキー
//   5 JUMP     — TAB 長押し → 右手上段=⌃⌥1..5（Dia スペース、Raycast
//                スクリプト経由でどこからでも）、ホーム段=⌘1..5（ピン/タブ）
//
// 旧 FUNCTION(1)・旧 MOUSE(4)・旧 Sym_Num(7) は到達不能または統合により削除。
// 矢印クラスタも削除（実測で Ctrl+IKJL(Karabiner) しか使われていない）。
// ─────────────────────────────────────────────────────────

&mt {
    flavor = "balanced";
    quick-tap-ms = <0>;
};

&sl { release-after-ms = <250>; };

&lt {
    tapping-term-ms = <150>;
    flavor = "balanced";
    hold-while-undecided;
    hold-while-undecided-linger;
}; // ダブルクリックに必要な時間を設定
&trackball {
    // オートマウスレイヤーは無効（有効化するなら専用レイヤーを追加して番号を指定）

    scroll-layers = <3>;
};

/ {
    combos {
        compatible = "zmk,combos";

        copy-chatGPT {
            bindings = <&select_copy LG(A) LG(C)>;
            key-positions = <27 26>;
        };

        question {
            bindings = <&kp QUESTION>;
            key-positions = <21 20>;
        };

        alfred {
            bindings = <&kp LC(SPACE)>;
            key-positions = <10 38>;
        };

        shift_command_V {
            bindings = <&kp LS(LG(V))>;
            key-positions = <22 37 25>;
        };

        shift_command_C {
            bindings = <&kp LS(LG(C))>;
            key-positions = <22 37 24>;
        };

        shift_command_E {
            bindings = <&kp LS(LG(E))>;
            key-positions = <22 37 2>;
        };

        shift_command_S {
            bindings = <&kp LS(LG(S))>;
            key-positions = <22 37 11>;
        };

        shift_command_D {
            bindings = <&kp LS(LG(D))>;
            key-positions = <22 37 12>;
        };

        apple {
            bindings = <&Apple_macro>;
            key-positions = <18 41 19 20 21>;
        };
    };

    macros {
        browser_back: browser_back {
            compatible = "zmk,behavior-macro";
            #binding-cells = <0>;
            bindings = <&kp LG(LEFT_ARROW)>;
            label = "BROWSER_BACK";
        };

        zoom_in: zoom_in {
            compatible = "zmk,behavior-macro";
            #binding-cells = <0>;
            bindings = <&kp LG(PLUS)>;
            label = "ZOOM_IN";
        };

        select_copy: SELECT_AND_COPY {
            compatible = "zmk,behavior-macro-two-param";
            #binding-cells = <2>;
            bindings =
                <&macro_press>,
                <&macro_param_1to1 &kp MACRO_PLACEHOLDER>,
                <&macro_pause_for_release>,
                <&macro_release>,
                <&macro_wait_time 50>,
                <&macro_press>,
                <&macro_param_2to1 &kp MACRO_PLACEHOLDER>,
                <&macro_pause_for_release>,
                <&macro_release>;

            label = "SELECT_AND_COPY";
        };

        Apple_macro: apple_macro {
            compatible = "zmk,behavior-macro";
            #binding-cells = <0>;
            bindings = <&kp LS(A) &kp P &kp P &kp L &kp E &kp N0 &kp N7 &kp N0 &kp N1>;
            label = "apple_MACRO";
        };
    };

    behaviors {
        // ── 実測に基づくチューニング（2026-08-07）──────────────
        // 2026-08-06 の実測 37,462 イベントから決めた値。根拠:
        //
        //   Z を hold して他キーを打った 271 件は押下時間が二峰性だった。
        //     0-120ms   78 件  相手は R/L  → tap のつもりが hold になった誤爆
        //     120-200ms  9 件  （谷）
        //     200ms 以上 184 件  相手は ENT 125 件 → 意図的な Shift+Enter・大文字
        //   意図的な hold の押下は p50 = 298ms。誤爆と 150ms できれいに切れる。
        //
        // flavor: balanced → tap-preferred
        //   balanced は「他キーを押して離した」時点で tapping-term 内でも hold を
        //   確定させる。誤爆 78 件のうち 62 件がこれで、tapping-term をいくら
        //   伸ばしても救えなかった。tap-preferred は tapping-term を超えるまで
        //   hold にしないので、この層が丸ごと消える。
        //
        // tapping-term-ms: 100 → 150
        //   素の物理キーの押下は p50 64ms / p90 104ms。100 では通常打鍵の
        //   11.7% が hold 側に落ちる。150 なら 1.5%。
        //
        // hold-while-undecided を削除
        //   押した瞬間に Shift がホストへ漏れ、tap 成立時も LSFT の down/up が
        //   macOS に届いていた（実測 98 件）。ことえりへの余計な Shift を止める。
        //
        // ※ position 34 は計測用に RIGHT_SHIFT のまま（macOS 上の機能は同じ）。
        //   Before/After を比べるため、効果確認が済むまで戻さない。
        // ──────────────────────────────────────────────────────
        mt_z_custom: mt_z_custom {
            compatible = "zmk,behavior-hold-tap";
            label = "MT_Z_CUSTOM";
            #binding-cells = <2>;
            flavor = "tap-preferred";
            quick-tap-ms = <0>;
            tapping-term-ms = <150>;
            bindings = <&kp>, <&kp>;

            retro-tap;
            hold-trigger-key-positions = <37 24 13 11 25 5 6 7 8 19 20 18 16 17 28 29 30 31 32 9 21 33 1 2 4 3 12 14 27 26 23 15 0 10 34 35 36 38 39 40 41>;
        };

        // 新しいスクロール動作の追加

        scroll_up_down: behavior_sensor_rotate_mouse_wheel_up_down {
            compatible = "zmk,behavior-sensor-rotate";
            #sensor-binding-cells = <0>;
            bindings = <&msc SCRL_DOWN>, <&msc SCRL_UP>;

            // 追加: スクロールのタップ時間を設定 (20ms)

            tap-ms = <20>;
        };

        mt_comand_custom: mt_comand_custom {
            compatible = "zmk,behavior-hold-tap";
            label = "MT_COMAND_CUSTOM";
            #binding-cells = <2>;
            flavor = "balanced";
            tapping-term-ms = <150>;
            bindings = <&kp>, <&kp>;

            quick-tap-ms = <0>;
            hold-trigger-key-positions = <22 10 36>;
        };

        new_behavior: new_behavior {
            compatible = "zmk,behavior-sensor-rotate";
            #sensor-binding-cells = <0>;
            bindings = <&msc SCRL_DOWN>, <&msc SCRL_UP>;

            tap-ms = <20>;
            label = "NEW_BEHAVIOR";
        };
    };

    keymap {
        compatible = "zmk,keymap";

        default_layer {
            bindings = <
&lt 4 Q                    &kp W         &kp E         &kp R                                 &kp T                                     &kp Y                    &kp U  &kp I     &kp O     &kp P
&mt LEFT_CONTROL A         &kp S         &kp D         &kp F                                 &kp G        &mkp MB3        &kp MINUS    &kp H                    &kp J  &kp K     &kp L     &lt 3 ENTER
&mt_z_custom LEFT_SHIFT Z  &kp X         &kp C         &kp V                                 &kp B        &kp PERIOD      &kp COMMA    &kp N                    &kp M  &mkp MB1  &mkp MB2  &kp BACKSPACE
&kp RIGHT_SHIFT            &kp LEFT_WIN  &kp LEFT_ALT  &mt_comand_custom LEFT_COMMAND LANG2  &lt 1 SPACE  &lt 5 TAB       &lt 2 SPACE  &mt RIGHT_COMMAND LANG1                             &kp ESC
            >;

            sensor-bindings = <&scroll_up_down>;
        };

        // 記号の家（旧 NUM ＋ 旧 Sym_Num を統合。左親指 SPACE 長押し → 右手で打つ）
        //
        // 最上段は JIS 数字段の Shift 記号ミラー（! " # $ % / & ' ( ) -）＝
        // 「2 の位置に "」という既存の暗記と一致させる（2026-08-14 ユーザー指定）。
        // ホーム段: ; : は多用するので人差し指・中指の一等地に隣接ペアで。
        // / (ためらい425ms) は薬指ホーム、@ (打ち直し12.9%) は人差し指の内寄せ。
        // [27][28] は trans ＝ default の . , がレイヤー内でもそのまま打てる。
        SYM_NUM {
            bindings = <
&kp EXCLAMATION  &kp JP_DQUOTE  &kp HASH  &kp DOLLAR    &kp PERCENT                                        &kp JP_AMPERSAND  &kp JP_QUOTE     &kp JP_LPAREN  &kp JP_RPAREN  &kp MINUS
&trans           &trans         &trans    &kp JP_PLUS   &kp JP_ASTERISK  &mkp MB3       &kp JP_UNDERSCORE  &kp JP_AT         &kp SEMICOLON    &kp JP_COLON   &kp SLASH      &kp JP_CARET
&trans           &trans         &trans    &kp JP_EQUAL  &kp JP_TILDE     &trans         &trans             &kp JP_LBRACKET   &kp JP_RBRACKET  &kp JP_LBRACE  &kp JP_RBRACE  &kp JP_BACKQUOTE
&trans           &trans         &trans    &trans        &trans           &trans         &trans             &trans                                                           &kp JP_PIPE
            >;

            sensor-bindings = <&scroll_up_down>;
        };

        // テンキー（旧 ARROW。右親指 SPACE 長押し → 左手で打つ）
        //
        // v2 変更: PERIOD → KP_DOT / COMMA → KP_COMMA。ことえりはテンキー入力を
        // 半角で通すので、かなモードのまま「3.5」が打てる（。事故の根絶）。
        // 右手の矢印クラスタ・HOME/END は削除（実測で Ctrl+IKJL しか使われていない）。
        // BACKSPACE だけ数字入力中の訂正用に残す。
        NUMPAD {
            bindings = <
&kp ESCAPE  &kp KP_NUMBER_7  &kp KP_NUMBER_8  &kp KP_NUMBER_9  &kp JP_YEN                            &trans  &trans         &trans  &trans  &trans
&kp SLASH   &kp KP_NUMBER_4  &kp KP_NUMBER_5  &kp KP_NUMBER_6  &kp JP_COLON   &mkp MB3       &trans  &kp BACKSPACE  &trans  &trans  &trans  &trans
&kp SLASH   &kp KP_NUMBER_1  &kp KP_NUMBER_2  &kp KP_NUMBER_3  &kp SEMICOLON  &trans         &trans  &trans         &trans  &trans  &trans  &trans
&trans      &kp KP_NUMBER_0  &kp KP_DOT       &kp KP_COMMA     &trans         &kp MINUS      &trans  &trans                                 &trans
            >;

            sensor-bindings = <&scroll_up_down>;
        };

        SCROLL {
            bindings = <
&kp C_VOLUME_UP    &kp C_BRIGHTNESS_DEC  &kp C_BRIGHTNESS_INC  &trans  &kp LA(LS(T))                                                        &kp LA(LS(Y))  &trans                  &kp LC(UP_ARROW)    &kp LA(LG(LS(L)))    &trans
&kp C_VOLUME_DOWN  &kp LC(LEFT_ARROW)    &kp LC(LEFT_ARROW)    &trans  &trans                      &kp LG(NUMBER_0)                 &trans  &trans         &kp LG(LA(UP_ARROW))    &kp LC(DOWN_ARROW)  &kp LC(RIGHT_ARROW)  &trans
&kp K_MUTE         &trans                &trans                &trans  &kp LC(LG(LA(LEFT_ARROW)))  &kp LA(LC(LG(RIGHT_ARROW)))      &trans  &trans         &kp LA(LG(DOWN_ARROW))  &kp LG(LEFT_ARROW)  &kp LG(RIGHT_ARROW)  &trans
&kp LA(LS(LG(M)))  &kp LA(LS(LG(V)))     &trans                &trans  &trans                      &trans                           &trans  &trans                                                                          &trans
            >;

            sensor-bindings = <&inc_dec_kp LG(MINUS) LG(JP_PLUS)>;
        };

        // システム（旧 layer_6。Q 長押し）: Bluetooth・bootloader に加えて
        // 旧 FUNCTION/旧 layer_8 から F1-F12 と < > を収容（実測ほぼ未使用のため隅で十分）
        SYS {
            bindings = <
&trans  &kp LESS_THAN  &kp GREATER_THAN  &trans        &trans                            &bt BT_SEL 0  &bt BT_SEL 1  &bt BT_SEL 2  &bt BT_SEL 3  &bt BT_SEL 4
&kp F1  &kp F2         &kp F3            &kp F4        &kp F5   &trans      &trans       &kp F6        &kp F7        &kp F8        &kp F9        &kp F10
&trans  &kp NUMBER_1   &kp NUMBER_2     &kp NUMBER_3  &kp F11  &kp F12     &bootloader  &trans        &trans        &trans        &trans        &bt BT_CLR
&trans  &trans         &trans           &trans        &trans   &trans      &trans       &trans                                                  &bt BT_CLR_ALL
            >;
        };

        // JUMP（新設。左親指 TAB 長押し → 右手で行き先を1打）
        //
        //   上段 Y U I O P   = ⌃⌥1..5 → Raycast スクリプト「Dia スペース N」
        //                      （どのアプリに居ても Dia を前面化してスペース切替）
        //   ホーム段 H J K L ENT = ⌘1..5 → Dia のピン留めアプリ。他アプリでも
        //                      「N番目」(Slack ワークスペース・ブラウザタブ等) に効く
        //
        // アプリ切替（⇧⌘E 等）はレイヤー化しない方針（2026-08-14 ユーザー判断。
        // ショートカットとして完成しており、通常キーボードとの筋肉記憶を共有するため）
        JUMP {
            bindings = <
&trans  &trans  &trans  &trans  &trans                      &kp LC(LA(N1))  &kp LC(LA(N2))  &kp LC(LA(N3))  &kp LC(LA(N4))  &kp LC(LA(N5))
&trans  &trans  &trans  &trans  &trans  &trans      &trans  &kp LG(N1)      &kp LG(N2)      &kp LG(N3)      &kp LG(N4)      &kp LG(N5)
&trans  &trans  &trans  &trans  &trans  &trans      &trans  &trans          &trans          &trans          &trans          &trans
&trans  &trans  &trans  &trans  &trans  &trans      &trans  &trans                                                          &trans
            >;
        };
    };
};
